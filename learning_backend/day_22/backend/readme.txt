Today (day 20) - 
    1. We learned about websocket protocol.
    2. Learned about socket.io library - which we are going to use for using websocket.
    3. Read some gemini api docs about how to implement short term memory in chats for chatbot.


Today (day 21) - 
    1. We learned and implemented a client (postman) and server connection with the socket.io server.
    2. We learned about events in websocket (in-built and custom)
    3. Also added gemini ai for the response from server to client.